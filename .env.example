# PostgreSQL connection string used by Prisma (cart & orders).
# Get a free serverless Postgres at https://neon.tech and paste the
# connection string here for local dev, and into Vercel's Environment
# Variables (Project → Settings → Environment Variables) for production.
DATABASE_URL="postgresql://user:password@host.neon.tech/dbname?sslmode=require"

# --- Admin dashboard (/admin) ---
# Password for the admin login. ALWAYS set a strong value in production.
# Defaults to "admin123" if unset (dev only).
ADMIN_PASSWORD="change-me-to-a-strong-password"

# --- New-order email notifications (optional) ---
# Uses Resend (https://resend.com). Leave RESEND_API_KEY empty to disable
# emails — checkout still works, notifications are simply skipped.
RESEND_API_KEY=""
ADMIN_EMAIL="you@example.com"
EMAIL_FROM="Import China <onboarding@resend.dev>"

# --- AR Virtual Try-On (sunglasses) ---
# Where face-api.js loads its model weights from. Defaults to a jsDelivr CDN
# (works out of the box). For best reliability self-host the weights in
# public/models and set this to "/models".
NEXT_PUBLIC_FACEAPI_MODELS=""

# --- CartBot AI assistant (/api/chat) ---
# Anthropic API key powers the chatbot (model: claude-sonnet-4-6). Without it,
# CartBot falls back to keyword product search and still works.
ANTHROPIC_API_KEY=""

# bKash PGW (tokenized checkout). Leave unset for manual TrxID confirmation.
# Sandbox base URL shown; bKash publishes sandbox test credentials.
BKASH_BASE_URL="https://tokenized.sandbox.bka.sh/v1.2.0-beta"
BKASH_APP_KEY=""
BKASH_APP_SECRET=""
BKASH_USERNAME=""
BKASH_PASSWORD=""
